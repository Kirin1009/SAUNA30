# Sauna470s（旧SAUNA 30） — プロジェクト引き継ぎ書（2026.08.06更新）

個人用サウナデジタルスタンプラリー。アプリ名は **Sauna470s**（改名は表示のみ。localStorageキー`sauna30:v2`・IndexedDB名・キャッシュ接頭辞は既存ユーザー保護のため旧名のまま）。オーナー：Shun（岐阜在住）。ユーザーは本人と妻・紗也乃の2名。
公開URL: https://kirin1009.github.io/SAUNA30/ （GitHub Pages, mainブランチ直デプロイ。マージ後1〜7分で反映）

## コンセプト

- サウナイキタイの**都道府県別イキタイ数TOP10を「枠」として固定**し、GPSで踏破していくコレクション帳。現在の県パックは**全国47都道府県470枠**（2026.08.06版）
- ランキング変動があっても枠は動かさない。更新・県追加は「新しい版」として発行する（押したスタンプが消えないことを保証する設計思想）
- 枠（決められた場所を踏む）と台帳（行った場所を全部残す）は**別の欲求**であり二層構造。台帳=枠外施設の自由登録、行きたい=ブックマーク
- **実データ主義**：枠の施設名・イキタイ数・座標は実物のみ。捏造データは絶対に入れない（座標が違うとGPSスタンプが壊れる）
- 将来構想：誰でも自分のスタンプラリーを作れる基盤への発展

## 採点思想（妻との議論で確定。変更しない）

- **4軸×25点**：サウナ室 / 水風呂 / 外気浴・休憩 / 清潔・雰囲気
- **＋特別加減点±10（理由の記入必須）**。基準線：竜泉寺の湯 ≒ 82点
- 導線・コスパ・唯一性は軸にしない（検討の末に却下）
- **整いは採点に含めない**。訪問ごとの★0.5刻み(0-5)で別記録
- 施設スコア＝最新の採点付き訪問の合計。タグは採点外の属性情報

## 技術構成

- **index.html単体完結**（依存ライブラリゼロ、ビルドなし）。この方針は維持する。静的画像（アイコン・判子帳の絵 `art/`）だけは別ファイル
- データ：localStorage `sauna30:v2`（visits/userFacilities/wants/tags/customTags/radius/**bath**）、写真はIndexedDB `sauna30`/`photos`（1280px/JPEG圧縮）
- 旧v1キー `sauna30-tokai:v1` からの移行処理あり（旧scoreはlegacyScoreとして「（旧）」表示）。**移行コードは消さない**
- 地図：**全国47都道府県対応済み**。`SHAPES` 定数は `{meta, biwa, prefs:{hokkaido..okinawa}}` 構造（約35KB）
  - 出典：dataofjapan/land の japan.geojson をDouglas-Peucker簡略化＋等角投影（cos補正）で変換
  - 沖縄は左上インセット（1.7倍）。`meta.okiS/okiAx/okiAy` で座標変換、`lng<128.5 && lat<27.5` の点はインセット側に写像される（renderMapのpx/py参照）。奄美・小笠原・北方領土は地図外
  - 琵琶湖は旧3県版SHAPESから逆投影→再投影したもの
  - 県塗り：PREFに定義がある県（=県パック）は県色、押印数で濃くなる。パック外の県は中間色 #C7A876
  - 県名ラベルは**ズーム時（zf≦0.55）のみ**表示。全国表示で47枚並べると潰れるため。置き位置は `prefAnchor()` が各県の最大サブパスの重心から自動算出（PREF未収録の県は出さない）
- FRAMEの1件が持つフィールド：`id/pref/rank/name/city/ikitai/sakatsu/lat/lng/sid`＋`m|w|u:[サウナ室温度,水風呂温度,外気浴,ロウリュ]`（男/女/共用、無い値はnull）＋`fee/hours/off/hp/note`。すべてサウナイキタイ由来で、営業情報は版の時点のスナップショット
- ヘッダーの進捗は**7地方の折りたたみ式**（`REGIONS` 定数）。地方をタップで県セルが開く。リストの県フィルタも地方→県の二段
- 全国表示ではピンのタップ判定半径を絞る（470枠だと重なって誤爆するため。拡大すれば従来の広さ）
- ズーム：最大64倍（clamp W/64）。**判子はズームするほど育つ**——倍率 `mk=min(3.3, 1.45*zoom^0.4)`（renderMap内）を画面一定サイズに掛ける。全国表示でも一回り大きく、zoom≧1.8で「岐2」の字を刷る。ホイールはデバウンス160ms・ピンチ/タップはpointerup時に `renderedW` と比較して再描画する方式
- 地図タップは**pointerupでelementFromPoint判定**（pointer captureがclickを奪うバグ回避。clickリスナーに戻さない）
- GPS：半径判定（設定100-1000m）。複数ヒット時は候補選択シート（栄はTOP10が3施設密集）
- PWA：manifest.json / sw.js / アイコン3種。SWはナビゲーションをネット優先、圏外時キャッシュ
- **バージョンは `APP_VERSION` の1箇所**（index.html、現在 `1.5.0`）。SWは `./sw.js?v=<APP_VERSION>` で登録され、sw.js側は `self.location` のクエリからキャッシュ名を組み立てる（`sauna30-1.0.0`）。**sw.jsを手で書き換える必要はない**。MAJOR=データ構造やlocalStorage互換の変更 / MINOR=機能追加 / PATCH=修正
- `EDITION`（枠データの版、現在 `2026.08.06版`）は `APP_VERSION` とは別物。サウナイキタイのランキングを固定した日付を指す。設定タブに両方を並べて表示している
- **利用する浴室**（`state.bath` = `"m"`/`"w"`/`null`）：男性専用53枠・女性専用2枠があるため、分母と県制覇の判定を各自に合わせる。男が入れる468枠 / 女が入れる418枠（愛知は女性5枠）。`bathOK(f)` / `framesOf(pref)` を通す。**押したスタンプ自体は消えない**（分母から外れるだけ）
- **記録の修正**：履歴と施設詳細から訪問の削除・編集・施設の付け替えができる。削除時は紐づく写真をIndexedDBからも消す（`photoDel`）。付け替えはGPSが隣の施設を拾ったとき用で、採点と写真はそのまま移る
- **判子帳（帳タブ）**：県ごとに県鳥の隠し絵を10ピースに割り、押した枠のピースだけめくれるパズル（`puzzleSVG()`）。絵は `art/<slug>.webp`（47枚・840×480・計約1.4MB。ChatGPT生成→Pillow でWebP化。生成指示書は scratchpad の sauna30-art-brief-v3.md 系譜）。シーンはサウナ室/水風呂/外気浴/露天風呂/サ飯の5種で、`BIRDS` は県鳥名のみ保持。未開ピースには開けるための判子名（岐7等）を刷る。枠外ピースは最初から開く。**開くまで絵柄は伏せる**。SWが47枚をプリキャッシュ（圏外対応）
- **段階制覇**：県→地方（`conqueredRegion`）→全国（`conqueredAll`）。地方制覇はヘッダーと帳に金バッジ
- **制覇証明書**：県/地方/全国の制覇で `certSVG()` から判子敷き詰めの証明書を生成、canvas経由でPNG保存できる
- **判子**：`sealText(f)` が「岐1」＝県の一文字＋順位を返す（台帳は「湯」）。押印アニメの大判・地図の朱印（拡大時のみ）・リスト/履歴のミニ判子で共有。県制覇すると金の「制」判が県の重心に重なり、押した瞬間は金の「制覇」大判が出る。制覇判定は `conquered(pref)`＝その人が入れる枠を全部押したとき（固定10ではない）
- バックアップ：設定タブから写真込みJSONの書き出し/読み込み。互換を壊さない（`bath` 未設定の旧ファイルも読める）
- **記録ファースト**（v1.5〜。「ログが一次・ラリーは副産物」の思想転換）：ヘッダー直下に**サ活サマリー**（今年のサ活/通算サ活/行った湯/常連の湯）。7地方の枠進捗グリッドは**帳タブの先頭**に降格。同一施設10回以上で金の**常連判**（詳細とリストに表示）、リストは2回以上で「◯回」表示。コンテンツリッチ化はチャッピー参加時に予定

## デザイン（ライトテーマ、2026.08.06完成）

- アイコン（サウナハットのキャラ、クリーム地・判子持ち）の手描き世界観にUI全体を統一
- パレット：地 #FEF6E6 / 面 #FFFFF8 / 線・文字 こげ茶 #4A3B2E / 判子赤 #D9483B（CSS変数 --stamp）/ 金 #C7973F
- 県色は彩度落としで**地方ごとに色family**を割り当てる（東北=藍 / 関東=藤 / 中部=山吹・土 / 近畿=青緑 / 中国=苔 / 四国=若草 / 九州=朱）。岐阜(山吹)#C7973F・愛知(朱)#C75B47・滋賀(青)#5E88A0 は据え置き。台帳#A69374
- フォント：丸ゴシック系（ui-rounded → Hiragino Maru Gothic ProN → Yu Gothic）
- スタンプ表現：地図=朱印風丸判子（feTurbulence+feDisplacementMapでかすれ）、台帳=角印(-8°)、リスト=「済」ミニ判子（.hankoクラス）
- チェックイン保存時に「湯」の判子アニメーション（#stampfx / stampFx()関数：押印→バウンド→定着→フェード、約2秒）
- 地図は暖色の紙地図調（下地#FBF1DC＋方眼グリッド、琵琶湖淡青）
- theme-colorメタ/manifestの色は #FEF6E6 に同期済み

## サ道場 東海（challenges/ 配下の姉妹アプリ・地方版）

- **別アプリ・1ファイル完結**（`challenges/index.html`）。独立PWA（manifest/sw別、キャッシュ名 `sadojo-<ver>`、localStorage `sadojo:v1`）
- 施設スナップショット149件（東海5県TOP30）を内蔵：470枠由来50件＋**新規99件**（id=`t<サウナイキタイid>`、SECRET SAUNA KAIHOは座標非公開のため除外）。各行 id/pref/座標/wmin/smax/fee/tag(y=予約 t=宿泊 n=深夜)/k(施設タイプ: onsen/sento/private/capsule/hotel/camp/other)
- **地方版**。コンセプトは「東海地方のサウナを極める」。全国版は後回しで、`REGION` 定数と FAC を差し替えれば関東版などを発行できる型にしてある
- お題8本：銭湯11・プライベート17・高級(≧3000円)25・深夜(翌2時〜/24h)19・シングル15・灼熱番付35(段位制)・予約の名店30から10・**総仕上げ「東海のサウナを極める」全149湯**
- 深夜判定は営業時間文字列の `24時間|翌0?[2-9]|翌1[0-9]`。施設タイプは一覧ページ由来（「温浴施設（スーパー銭湯…）」を銭湯と誤分類しないよう**先頭一致**で判定）
- **東海のサウナログ二層構造**（v2.4〜）：道場/地図の2タブ。スタンプ＝施設なので**施設カード**（`openFac()`）に押印・行きたい・Googleマップ・登録削除を集約。お題の行タップでカードが開き、そこから手動押印（**長押し押印は廃止**。スクロール誤爆の温床だった）
- **地図タブ**：SHAPESは470s全国版から**東海5県ぶんを抜粋した同じ投影座標**（約4KB、`TBOX` が初期ビュー＝東海全体、最大32倍ズーム）。ピン=丸（枠149・押印済みは朱印、行きたいは金縁）と角（自分の登録施設）。タップ判定は470sと同じ**pointerup+elementFromPoint**方式。琵琶湖も描く
- `sadojo:v1` に `wants`（行きたい）と `ufacs`（枠外の自由登録施設 `{id:"u<ts>",name,lat,lng,u:1}`）を追加。**旧データは既定値で埋まる**（後方互換）。ufacはお題に数えないが押印数と地図に出る。登録は地図タブ「現在地で施設を登録」＝GPS必須・登録と同時に押印。既存施設が250m以内なら警告
- 押印はGPS（お題対象のみ）と施設カードの手動押印。**Sauna470sのバックアップJSONを読み込むと押印が一括反映**（一方向。逆流なし）
- **施設判子の自動生成**（v2.5〜、`facSeal()`）：149湯すべてが違う判子。形=施設タイプ（丸=温浴・銭湯/角=貸切・カプセル/六角=宿/五角=アウトドア）、中央=頭文字、肩=市町村、モチーフ=実データ（炎=サ室100°C・双炎=110°C/雪=シングル/月=深夜/鍵=予約制/小判=¥3,000以上）。**モチーフ3個以上は下弧に扇状配置**（勲章式・破綻しない）。かすれは共有フィルタ `#sealtex` 1個（149個並べても軽い）、傾きはidハッシュ。押印演出もその施設の判子が出る（開封の瞬間）。**判子帳タブ**は道場/判子帳/地図の3タブ目で、未踏は形と「？」だけ見せて伏せる。グリッドは `minmax(0,1fr)`（SVGの固有幅でセルが巨大化するのを防ぐ）
- **お題ごとの隠し絵（武将タイル画）**：`challenges/art/st-<お題id>.webp` 14枚（840×480・計約0.9MB。チャッピー生成の戦国武将×サウナ。主役=道三、濃姫・信長はサブ）。お題詳細の上部で10ピースのパズルになっており、進捗割合でめくれる（`tileArt()`）。次のピースには「あと◯湯でひらく」。SWがプリキャッシュ
- **武将キャラ10人**：`challenges/art/ch-<名>.webp`（240×240・計57KB。チャッピー生成の立ち姿。道三/濃姫/信長/秀吉/家康/九鬼/光秀/義元/長政/お市）。`CHARS` 定数が名前を持ち、各お題の `chr` が師範を指す（お題詳細に丸アバター表示。配役はタイル画のシーンに合わせた）。ページ下部に「道場の面々」ギャラリー。SWがプリキャッシュ
- **採点**（v2.7〜、v2.8で改定）：カードの手動押印で採点画面に遷移。**5軸（サウナ室/水風呂/外気浴・休憩/清潔・雰囲気/導線）×5点満点・0.1刻み＋特別加減点±2.5・理由必須**。基準線：竜泉寺の湯≒20.5点。採点せず押印も可、後からカードの「採点する」でも入れられる。`sadojo:v1` の `scores:{fid:{s:[5軸],x,why,v:2}}`。**旧4軸×25点データは読み込み時に換算**（÷5、x÷4、導線=2.5、v:2付与）。押印取り消しで採点も消える。GPS押印は即押印（採点は後から）。※470s本体は4軸×25点のまま——**サ道場側だけ導線軸を採用**（オーナー了承の上で非整合）。**合計は100点換算表示**（×4。竜泉寺≒82点。保存は5点満点のまま）。採点画面に**支払った料金**欄（任意、`paid:{fid:円}`。採点スキップでも保存、押印取り消しで消える。コスパは採点軸にしない思想のまま属性情報として記録）
- 満願で「満願之証」をPNG保存できる。**証書は満願で完成したそのお題の武将絵を下絵に敷き、実際に押した施設判子を押した順で敷き詰める**（絵はdata URI埋め込みでPNGにも焼き込まれる）。満願判は金
- **お題の家紋スタンプ**（v2.9〜）：`challenges/art/km-<お題id>.webp` 14枚（256px黒/透過・計137KB。チャッピー画像生成、sadojo-kamon-brief-v3.md。SVGコード生成は2回失敗して放棄した経緯あり）。県=実在紋（岐阜=撫子/愛知=織田木瓜/三重=九鬼七曜/静岡=三つ葉葵/滋賀=三つ盛亀甲）、特性=創作サウナ紋。**CSSマスク方式**（`.kmz`＝mask-image＋background:currentColor）でお題カード=紺・満願=金・押印済み行/カードのミニ判=朱に染め分け。七曜の円7つ・湯気5本（=東海5県）は検品済み。SWがプリキャッシュ
- 更新時は challenges/index.html の APP_VERSION を上げる（sw.jsは自動追従）
- 次の候補：他地方のTOP30を追加して「関東の部」等を発行（東海と同じ手順。tokai30.py系のクロールで約20分/地方）

## ととのい帳（totonoi/ 配下・次期統合アプリ。現在アルファ版）

- **御朱印帳コンセプトの統合後継**（docs/要件定義-御朱印帳.md **v0.4が憲法**。承認ビジュアルターゲット4画面から逆算。v0.4で「御朱印帳×みんなで作るサウナカタログ」に拡張——データ三層（公式/観測/体験）・判子が観測で育つ・探すは記録の後。公式カタログ層の自前化は別リポジトリ **Kirin1009/Search-saunas** で進行中）。470sとサ道場は将来ここへ誘導予定（現状は3アプリ並存）。第三者分析レポート（docs/ととのい帳-分析レポート-20260814.md）の最重要指摘=データ保全。**次の実装はバックアップ形式の設計**（データJSON＋無圧縮ZIP写真・schema version・storage.persist）が最優先
- **別アプリ**（`totonoi/index.html` 約97KB＋施設カタログ `totonoi/catalog.js` 約172KB）。独立PWA（キャッシュ `totonoi-<ver>`、localStorage `totonoi:v1`）
- **施設カタログの分離配信**（alpha.18〜。1ファイル主義の再定義＝本体1ファイル＋静的データファイル）：`catalog.js` の中身は `window.CATALOG=<純JSON>;` の1行（先頭と末尾を剥がせばJSONとしてパース可能）。メタ `{app,catalog:第n版,edition}` ＋ FACILITIES/PREF/REGIONS/SHAPES。**SWはcatalog.jsだけネット優先**なので、アプリ本体・SWを触らずカタログの版だけ上げられる（Git=実質DB。Search-saunasのスキーマv2受け皿）。圏外はキャッシュで動く。EDITIONはカタログ由来になった。再生成は scratchpad の split-catalog.py 系譜（凍結アサート：569湯・470枠・id重複なし・座標型）
- **統合施設マスタ569湯**：470s全国470枠＋サ道場東海の新規99湯（id=`t<サウナイキタイid>`）。生成時にスキーマ正規化——FRAMEからは wmin=男女共用の最低水温 / smax=最高サ室温 / n=深夜（hours正規表現）を導出、東海50重複枠はidそのまま＋サ道場のk/y/tを流し込み。**凍結アサート**（枠のid/座標/イキタイ不変）を通してから埋め込み。銭湯/貸切の絞り込みは施設タイプ`k`があるもの（=東海149）のみ効く点に注意
- データ：`state.visits[]`＝`{id,fid,date,sc:{s:[5軸],x,why,v:2}|null,paid|null,note}` 新しい順。1訪問=1レコード（同じ湯に何度でも）。wants/ufacs/radius
- **4タブ**：帳（パスポートページ：判子が左右交互に時系列、最新だけ大判＋メモ、月区切り、再訪は薄い幽霊判＋◯回目短冊。タップで訪問詳細カード）／探す（チップ絞り込み＋全国SHAPES地図＋近い順リスト。ピン=朱ベタ/白抜き/金縁）／棚（地方→県の木の棚、伏せ判、回数バッジ、10回金）／記録（時系列リスト・手動記録の施設検索・取り込み・設定）
- 押印フロー：GPS or 施設ページ「ここで押印」→ visit追加＋押印演出 → **採点シートへ自動遷移**（5軸0.1刻み＋特別±2.5・100点換算・支払・ひとこと。スキップ可）。半径外はその場で枠外登録→押印
- **取り込み（お試し）**：同一オリジンなので470s `sauna30:v2` とサ道場 `sadojo:v1` の localStorage を直読みしてワンタップ変換（470sの旧4軸→5軸換算、冪等。写真は未対応=正式版でIndexedDBごと）
- 御朱印＝facSeal移植版（k無し施設は丸。金=常連10回）。かすれ共有フィルタ #sealtex
- **スキン素材第1弾組み込み済み**（alpha.2、チャッピー納品PR #29→原本は `assets/totonoi/home-skin/`、runtime用は `totonoi/art/` に複製）：和紙テクスチャ（#bookの下地・256pxタイル）、和綴じ帯（左端repeat-y 26px）、ページ区切り=前ページの覗き（.pgdiv）、タブアイコン4種（CSSマスク方式）、専用アプリアイコン（icon-192/512・apple-touch。紙色に合成済み）。**金の栞素材は栞（リワード）実装まで温存**
- **探すの地図改善**（alpha.3）：地方→県の二段セレクトでズーム＋リスト絞り込み。ピンのタップは**描画順でなく最寄りピンの数学判定**（`pickPin()`・mapPins配列）、指の範囲に複数重なれば候補シートから選ぶ。**スキン第2弾組み込み済み**（alpha.4、PR #32→原本 `assets/totonoi/find-skin/`）：紙地図下地（`#mapbox`のCSS背景=ズームしても紙目一定。SVG側のベタ塗りrectは削除済み）、特性アイコン4種 雪/月/桶/鍵（`.ft` CSSマスク方式でリスト行とチップに表示）。ピン意匠見本（滴型3態）は未実装の参考資料。**スキン第3弾組み込み済み**（alpha.5、PR #34→原本 `assets/totonoi/shelf-skin/`）：棚タブを木の棚に——背板=淡い木肌・県ごとに飴色の棚板（`.plank`）・最下段の飾り棚にしつらえ小物（盆栽/一輪挿し/湯呑みを地方indexで1点）。これで承認4画面の質感素材は一巡
- **帳リッチ化ラウンド**（alpha.6〜8、チャッピーのデザインレビュー準拠）：最新の一印を主役に（最新判126px濃く/過去判80px薄く・乱れは訪問IDハッシュで決定的）、5軸ミニレーダー（radarSVG・各軸に点数）、総合点の星0.5刻み（starsSVG・(点-40)/10丸め）、**スキン第4弾＝紙造作**（PR #38→原本 `assets/totonoi/book-skin/`。上端紙層/下端の紙の重なり=#book背景レイヤー、右小口repeat-y、月ブロックを`.pg`セクション化して最新月`.cur`の右下にめくれ、`.pe-nth`の短冊=visit-slip）。手書き文字はOFLサブセットフォント埋め込み方針（未実装）。写真セット（ポラロイド枠・マステ）は写真機能の仕様確定後に第5弾
- **チャッピーへの素材依頼の規約**（Issue #24）：コメント**1行目は依頼印`【チャッピー依頼】`のみ・題名は2行目から**（チャッピーの監視が先頭行完全一致のため。同じ行に題名を続けると検知漏れ＝実績あり）。投稿後はShunがCodex側でタスクを起動すると確実。納品はDraft PR方式（原本`assets/totonoi/<画面>-skin/`＋README、組み込みはココ）。画面は1枚ずつ順に依頼
- **バックアップ実装済み**（alpha.10）：記録タブに書き出し/読み込み。形式=`{app:"totonoi",schema:1,exportedAt,appVersion,state}`。**読み込み側は全過去schemaを受理し続ける**（絶対ルール）。書き出しはWeb Share（iOSでiCloud Drive/ファイルへ直接）→不可ならダウンロード。`state.bk={at,visits}` で未保全件数を追跡し、起動時に未保全5件以上 or 30日経過で書き出しを促す。`storage.persist()` も起動時に要求。**写真は将来「データJSON＋無圧縮ZIP」の分離形式**（JSONに同梱しない。分析レポートR4）
- **写真実装済み**（alpha.11〜12）：1訪問1枚・訪問詳細から添付（押印フローに載せない）。1280px JPEG圧縮→IndexedDB `totonoi`/`photos`（key=訪問id、`v.ph`フラグ）。表示=きょうの欄の大判ポラロイド＋履歴行46pxサムネ（第5弾素材 polaroid-frame/tape-washi 2種・窓位置は実測%）。**写真バックアップは分離ZIP**（無圧縮store方式・自前実装。`<訪問id>.jpg`、Web Share対応、central directory解析で復元）
- **栞実装済み**（alpha.12）：県/地方/全国制覇で金の栞（ribbon-gold.webp）が達成ページに挟まり、**次回起動時に一回だけ祝いのシート**（金の制判）。判定=`calcShiori()`（枠=f.rank持ちのみ・最初の訪問日で達成日を決定）、既読=`state.shioriSeen`（バックアップに含む）。進捗の事前表示はしない原則を維持
- **常連の間実装済み**（alpha.13）：施設ページの訪問史を拡張——採点推移の折れ線（走り書きスパークライン）、**初回/前回/今回の三面鏡カード**（日付・写真サムネor伏せ判・点数・ひとこと、タップで訪問詳細）、ひとこと帳（日付付き直近4件）。見出しは10回以上で「常連の間」に変わる
- **観測層の第一歩実装済み**（alpha.15）：施設ページに「わたしの観測」——自由タグ（候補チップ＋手入力、`state.obs={fid:{tags,memo,at}}`）と観測メモ。サ室の数・設備・気づきを施設単位で書き残せる（スキーマv2 §5の運用開始。判子が育つ・検索連動は次段）。バックアップに含む。採点シートからは誘導文のみ（記録ファースト維持）
- **観測温度＝判子が育つ実装済み**（alpha.17）：採点シートのサ室/水風呂軸の🏷パネルに実測温度欄（カンマ区切りで複数室「101,86」→`obs.ax[i].temps`。-30〜150°Cでフィルタ・即保存・採点とは独立）。`obsTemps()`で**観測が公式値に勝ち**（要件定義§10.3）、`sealBadges()`はマージ値で判定——**2室100°C以上で炎→双炎に昇格**（部屋数で育つ第一歩）。施設ページ観測節に温度バッジ（タップで削除、`__obsTempDel`）。obsは丸ごとバックアップ対象なので温度も自動で含まれる
- **アルファの未実装（正式版v2.0.0まで）**：記憶をめくり返す（1日1件の紙片・年次扉）、観測タグ・温度での検索連動、県鳥パズル・証明書の移植、夫婦比較、既存2アプリからの誘導バナー、totonoi:v2ストア移行

## 県パックの更新（次にやるなら）

全国47都道府県ぶんは2026.08.06版で収録済み。次は「ランキングが動いたときの新しい版の発行」か、TOP10より下の順位まで枠を広げるかの検討になる。**既存の枠は動かさず追加する**という原則は変わらない。

**この環境はネットワーク設定で sauna-ikitai.com が許可済み**（もし403 CONNECT拒否が出たら環境のNetwork access設定を確認）。

### データの取り方（2026.08.06に確立）
1. 県別検索 `https://sauna-ikitai.com/search?prefecture[]=<slug>&sort=ikitai` の1ページ目＝TOP10。slugは `SHAPES.prefs` のキーと完全一致する
   - 一覧から取れる：施設名 / イキタイ数 / サ活数 / 市区町村 / 施設タイプ / 最安料金 / **男女別のサウナ室温度・水風呂温度・外気浴・ロウリュ** / 事前予約制などの注記
2. 座標は施設ページ `https://sauna-ikitai.com/saunas/<id>` の `<div id="map" data-lat data-lng>` から。公式HP・定休日・曜日別営業時間も同じページ
3. id規約：既存3県は `g/a/s`+順位で凍結、それ以外は **slug+順位**（`hokkaido1`, `mie1`）
4. `PREF` は `{label, short, color}`。`short` は2文字（山形/山梨/山口、福井/福岡/福島が頭文字で衝突するため）
5. `EDITION` 更新、sw.jsのCACHEバンプ、Playwright 390pxで確認、ブランチ→PR

### ハマりどころ（実測済み。同じ道を辿らないこと）
- **AWS WAFチャレンジ**が出る。`x-amzn-waf-action: challenge` の202が返り本文が空になる。トークンを抜いて回避するのは筋が悪いので、curlにcookieジャーを持たせて**5秒間隔でリトライし通る分だけ拾う**。470ページで約75分、実効6〜30秒/件。一覧ページは通りやすく `/saunas/<id>` が詰まる。**間隔を詰めても総スループットは変わらない**（サーバ側がトークンバケット的な制限をしている）
- **ジオコーディングで座標を代替するのは不可**。正しい住所を渡しても国土地理院は 75m〜2168m ばらつく（大垣サウナは住所文字列が完全一致でも2.1kmズレる）。施設名で引くと `ニコーリフレ SAPPORO → 千葉県旭市ニ` のように頭文字を地名として拾い800km外す。Nominatimは当たれば3〜51mと正確だが25件中3件しかヒットしない（OSMに日本のサウナPOIが無い）。**返ってくるので成功に見えるのが一番危険**。一覧ページに番地入り住所は無いので「住所で引く」も成立しない
- **既存枠との突き合わせは施設名ではなく座標で行う**（250m以内）。サウナイキタイ側で表記ゆれ（`SaunaLab Nagoya` → `SaunaLab Nagoya (サウナラボ名古屋)`、`竜泉寺の湯 名古屋守山本店` → `天空SPA HILLS 竜泉寺の湯 名古屋守山本店`）と順位入れ替え（滋賀9位/10位）が起きており、名前一致だと既存idを取りこぼしてスタンプが孤立する
- 生成スクリプトは「id重複なし」「既存30件のid・名称・座標・イキタイ数が不変」をアサートしてから書き出すこと。凍結30件の参照元は**変更前のmain（162d3ef）のindex.html**を使う（現行ファイルは形式が変わっているため）

## 変更時の絶対ルール

1. データ形式・localStorageキー・IndexedDB構成・バックアップ互換を壊さない
2. 既存FRAME 30件（id・座標・イキタイ数）は変更しない。追加のみ可。SHAPESの再生成は不要（47県組み込み済み）
3. 外部ライブラリ・外部フォント・ビルド工程を導入しない（1ファイル主義。静的画像 `art/` と、ととのい帳の施設カタログ `totonoi/catalog.js`＝データのみ・同一オリジン静的配信、は例外）
4. リリース時は `APP_VERSION` を上げる（sw.jsのキャッシュ名は自動追従するので触らない）
5. スマホ幅390pxで確認する（PlaywrightはPLAYWRIGHT環境変数設定済みのchromiumが使える）
6. 枠データの捏造禁止（実データ主義）

## ロードマップ

1. ✅ v1（点灯ドット版）→ v2（二層構造・4軸採点・写真・ズーム地図）→ PWA化
2. ✅ ライトテーマ全面リデザイン＋手描きアイコン（PR #1, #2）
3. ✅ 全国地図化・枠パック差し替え基盤（PR #3）
4. ✅ 県パックのデータ取得・追加（全国47都道府県470枠。2026.08.06版）
5. ✅ 記録の修正・浴室別の分母・判子のコレクション化
6. ✅ 判子帳（県鳥パズル・段階制覇・制覇証明書）
7. → **夫婦データの比較ビュー（次はここ）**（相手のバックアップJSONを読み込んで施設ごとに並べる）
8. → Capacitorでのネイティブ化とApp Store申請（審査4.2対策にローカル通知等を1つ足す想定）
9. → 枠差し替え可能なスタンプラリー自作基盤（DB導入はこの段階で初めて検討）

## 既知の注意点

- 大垣サウナと田辺温熱は約1.7km近接。全体表示ではタップ判定が重なる（ズームで分離、仕様として許容）
- SHIGARAKI SAUNAは宿泊者限定、田辺温熱/ITADORI/8Seasは事前予約制（noteに記載済み）
- iOSはapple-touch-iconを強くキャッシュ。アイコン変更時はホーム画面から削除→再追加が必要（アプリ内データはlocalStorage/IndexedDBなので消えない）
- GitHub操作：この開発環境ではgh CLI不可。git pushは可（GitHub Appインストール済み）、PR作成/マージはGitHub MCPツールで行う
- サウナイキタイはbot対策があるため、許可済み環境でも取得に失敗する場合はオーナーの「ページ全選択コピペ」方式にフォールバックする
